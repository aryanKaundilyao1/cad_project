export class GraphContextBuilder {
  /**
   * Assembles the required sub-graph based on the user's implicit permission scope.
   */
  static buildContext(baseGraph: any, permissionScope: any) {
    // MOCK: filter the graph based on permissions
    return baseGraph;
  }
}
