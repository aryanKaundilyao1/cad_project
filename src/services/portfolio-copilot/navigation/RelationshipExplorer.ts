export class RelationshipExplorer {
  /**
   * Returns all dependencies and influencers for a given node.
   */
  static getRelationships(graphNodeId: string) {
    // MOCK: get edges specific to dependency/influence
    return [];
  }
}
